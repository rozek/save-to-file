{
  "name": "save-to-file",
  "version": "0.1.0",
  "description": "a Svelte preprocessor that writes the results of any previous preprocessing into a file",
  "main": "./dist/save-to-file.js",
  "types": "./dist/save-to-file.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc && rollup -c rollup.config.js && mv src/*.d.ts dist && rm src/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rozek/save-to-file.git"
  },
  "keywords": [
    "svelte3",
    "svelte-preprocess"
  ],
  "author": "Andreas Rozek <a.rozek@gmx.de> (https://www.rozek.de/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rozek/save-to-file/issues"
  },
  "homepage": "https://github.com/rozek/save-to-file#readme",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^19.0.0",
    "@rollup/plugin-node-resolve": "^13.0.0",
    "@rollup/plugin-typescript": "^8.2.1",
    "rimraf": "^3.0.2",
    "rollup": "^2.52.7",
    "rollup-plugin-terser": "^7.0.2",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "javascript-interface-library": "^0.1.11"
  }
}
